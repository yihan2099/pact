{
  "name": "@porternetwork/indexer",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "dev:indexer": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target node",
    "start": "bun run dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@porternetwork/contracts": "workspace:*",
    "@porternetwork/database": "workspace:*",
    "@porternetwork/ipfs-utils": "workspace:*",
    "@porternetwork/shared-types": "workspace:*",
    "@porternetwork/web3-utils": "workspace:*",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.3"
  }
}
